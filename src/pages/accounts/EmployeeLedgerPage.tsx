import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{PageHeader}from"@/components/layout/PageHeader";import{Card,CardContent}from"@/components/ui/Card";import{employeeFinanceService}from"@/services/employeeFinanceService";import type{EmployeeFinanceSummary}from"@/types/claimFinance";import{formatCurrency}from"@/utils/format";export function EmployeeLedgerPage(){const[rows,setRows]=useState<EmployeeFinanceSummary[]>([]);useEffect(()=>void employeeFinanceService.summaries().then(setRows),[]);return <><PageHeader title="Employee Ledger" description="Advances, claims, payments and outstanding employee balances." breadcrumbs={[{label:"Accounts",to:"/accounts"},{label:"Employee Ledger"}]}/><Card><CardContent><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr>{["Employee","Opening/Advance","Submitted","Approved","Deducted","Paid","Available","Outstanding"].map(h=><th className="p-3 text-left" key={h}>{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.employeeId}><td><Link className="text-brand-blue" to={`/accounts/employee-ledger/${r.employeeId}`}>{r.employeeName}</Link></td>{[r.advance,r.submitted,r.approved,r.deducted,r.paid,r.availableBalance,r.outstanding].map((v,i)=><td key={i}>{formatCurrency(v)}</td>)}</tr>)}</tbody></table></div></CardContent></Card></>}
