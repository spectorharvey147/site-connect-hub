import{Activity}from"lucide-react";import type{ClaimAuditEvent}from"@/services/auditService";import{formatCurrency}from"@/utils/format";export function ClaimAuditTimeline({events}:{events:ClaimAuditEvent[]}){if(!events.length)return <p className="text-sm text-text-secondary">No additional audit events.</p>;return <ol className="space-y-3">{events.map(e=><li key={e.id} className="flex gap-3"><span className="mt-1 rounded-full bg-brand-light p-2"><Activity className="h-4 w-4 text-brand-blue"/></span><div className="flex-1 rounded-md border border-surface-border p-3"><div className="flex justify-between gap-2"><b>{e.action.replace(/[._]/g," ")}</b><small>{new Date(e.createdAt).toLocaleString()}</small></div><p className="text-sm text-text-secondary">{e.actorName??"System"}{e.actorRole?` · ${e.actorRole}`:""} · {e.source??"web"}</p>{e.remarks?<p className="mt-1 text-sm">{e.remarks}</p>:null}{e.amountBefore!==undefined?<p className="text-xs">Amount {formatCurrency(e.amountBefore)} → {formatCurrency(e.amountAfter??e.amountBefore)}</p>:null}</div></li>)}</ol>}
